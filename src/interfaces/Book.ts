export interface BookProps {
    id: string;
    name: string;
    author: string;
    volume: string;
    category: string;
    createdAt: string;
    imageUrl: string;
}

export interface BookCardProps {
    name: string;
    author: string;
    volume: string;
    category: string;
    imageUrl: string;
}
